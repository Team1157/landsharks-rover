websockets  # For communication between base station and rover, and between driver station and base station
pyserial-asyncio  # For communication between Raspberry Pi and Arduino on rover
bcrypt  # For password hashing and checking
click  # For `rover_user` command-line utility